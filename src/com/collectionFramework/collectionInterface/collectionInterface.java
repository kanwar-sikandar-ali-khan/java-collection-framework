package com.collectionFramework.collectionInterface;

public class collectionInterface {


}



//Collection Interface Methods:
//        ─────────────────────────────────────────────
//int size(); → Returns number of elements.
//boolean isEmpty(); → Checks if empty.
//boolean contains(Object o); → Checks if element exists.
//Iterator<E> iterator(); → Returns iterator.
//Object[] toArray(); → Converts to array.
//<T> T[] toArray(T[] a); → Converts to typed array.
//boolean add(E e); → Adds element.
//boolean remove(Object o); → Removes element.
//boolean containsAll(Collection<?> c); → Checks if contains all.
//boolean addAll(Collection<? extends E> c); → Adds all elements.
//boolean removeAll(Collection<?> c); → Removes all matching elements.
//boolean retainAll(Collection<?> c); → Keeps only matching elements.
//void clear(); → Removes all elements.
//default Spliterator<E> spliterator(); → Supports parallel iteration.
//default Stream<E> stream(); → Creates sequential stream.
//default Stream<E> parallelStream(); → Creates parallel stream.

